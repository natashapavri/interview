Subarray -
1. Subarray of length k and sum = x
	a. Sliding Window (Unsorted and sorted array)
	b. BST traverse (Sorted)
2. Subarray of sum = x
	a. Prefix sum hashmap
	b. two pointers
3. If you dont know the sum and find maximum sum subarray (negative+positive numbers)
	a. max_as_of_here and max_so_far approach